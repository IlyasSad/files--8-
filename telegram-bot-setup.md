# Инструкция по настройке Telegram бота для игры

## Шаг 1: Создать Telegram бота

1. Открой Telegram и найди @BotFather
2. Напиши `/newbot`
3. Введи имя бота (например: "Tic Tac Toe Promo Bot")
4. Введи username бота (например: "tictactoe_promo_bot")
5. BotFather даст тебе **токен** - сохрани его! Выглядит как: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

## Шаг 2: Получить Chat ID

### Вариант А (через личные сообщения):
1. Напиши своему боту любое сообщение (например: "Привет")
2. Открой в браузере: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
   (замени `<YOUR_BOT_TOKEN>` на свой токен)
3. В ответе найди `"chat":{"id":123456789` - это твой **chat_id**

### Вариант Б (через группу):
1. Создай группу в Telegram
2. Добавь своего бота в группу
3. Напиши что-то в группе
4. Открой: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
5. Найди chat_id (будет отрицательным, например: -987654321)

## Шаг 3: Вставить данные в код

Открой файл `tic-tac-toe-game.html` и найди эти строки:

```javascript
const TELEGRAM_BOT_TOKEN = 'YOUR_BOT_TOKEN'; // Замени на токен твоего бота
const TELEGRAM_CHAT_ID = 'YOUR_CHAT_ID'; // Замени на свой chat ID
```

Замени:
- `YOUR_BOT_TOKEN` на токен от BotFather
- `YOUR_CHAT_ID` на твой chat ID

**Пример:**
```javascript
const TELEGRAM_BOT_TOKEN = '7890123456:AAHdqTcvbXYZ1234567890abcdefghijk';
const TELEGRAM_CHAT_ID = '123456789';
```


## Шаг 4: Протестировать

1. Открой ссылку на игру
2. Сыграй партию
3. При победе или проигрыше проверь Telegram - должно прийти уведомление

## Что делать, если не работает:

### Telegram не отправляет сообщения:
1. Проверь, что токен и chat_id правильные
2. Открой консоль браузера (F12) - там будут ошибки если есть
3. Убедись, что сайт работает по HTTPS (не file://)
4. Напиши боту хотя бы одно сообщение вручную перед тестированием

### Игра не открывается:
1. Проверь, что файл называется `index.html`
2. Проверь консоль браузера на ошибки

## Бонус: Тестирование без деплоя

Если хочешь протестировать локально с HTTPS:
```bash
# Установи Python (если нет)
# Запусти простой HTTPS сервер:
python -m http.server 8000
```
Но Telegram всё равно не сможет получить уведомления из localhost.

---

## Готово! 🎉

Теперь у тебя:
✅ Работающая игра с красивым дизайном
✅ Telegram бот для уведомлений
✅ Публичная ссылка для тестового задания
